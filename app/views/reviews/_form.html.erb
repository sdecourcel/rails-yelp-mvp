<%= simple_form_for [restaurant, review] do |f| %>
  <%= f.input :rating, collection: [1,2,3,4,5], as: :radio_buttons, item_wrapper_class: 'radio-inline' %>
  <%= f.input :content %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
